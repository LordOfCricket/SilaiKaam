// Minimal local dev config placeholder.
// A dedicated env-loading solution (e.g. react-native-config) can be added
// when the app needs real per-environment builds.
export const API_URL = 'http://localhost:3001';
