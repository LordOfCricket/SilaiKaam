// Base Prisma schema for SilaiKaam.
// Intentionally empty of models — the final domain schema is designed in a later phase.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
